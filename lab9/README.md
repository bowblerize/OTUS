## Цель:

Написать bash-скрипт, который ежечасно формирует и отправляет на email отчёт о работе веб-сервера;

## Описание/Пошаговая инструкция выполнения домашнего задания:

🎯Что нужно сделать?

Написать скрипт для CRON, который раз в час формирует отчёт и отправляет его на заданную почту.

Скрипт должен предотвращать одновременный запуск нескольких копий, до его завершения.

## Отчёт должен содержать:

  IP-адреса с наибольшим числом запросов (с момента последнего запуска);
  
  Запрашиваемые URL с наибольшим числом запросов (с момента последнего запуска);
  
  Ошибки веб-сервера/приложения (с момента последнего запуска);
  
  HTTP-коды ответов с указанием их количества (с момента последнего запуска).
  

## Настройка для работы скрипта

Неободимо поставить пакеты mailutils, msmtp, msmtp-mta после создать в домашней директории файл .msmtprc и заполнить его следующим образом:
```
defaults
auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile        ~/.msmtp.log

account        gmail
host           smtp.gmail.com
port           587
from           user@gmail.com
user           user@gmail.com
password       password

account default : gmail
```
После нужно установить права 600 на этот файл

В скрипте указать значение переменных под ваши требования.

### Пример отчета

![Пример отчета](https://github.com/bowblerize/OTUS/blob/main/resources/lab9.png)
